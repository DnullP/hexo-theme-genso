
body {
    display: flex;

    flex-direction: column;

    justify-content: center;
    align-items: center;
    margin: 0;
    padding: 0;
}


@font-face {
  font-family: 'jf-openhuninn'; /* 自定义字体名称 */
  src: url('/font/jf-openhuninn-2.0.ttf') format('truetype'); /* 字体文件路径 */
}
